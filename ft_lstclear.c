#include "libft.h"
/*
void ft_lstclear(t_list **lst, void (*del)(void *))
{
    t_list *ptr = 0;
    t_list *temp;

    ptr->next = *lst;
    if (!lst)
        return;
    ptr = lst;
    while (ptr->next != 0)
    {
        del(temp->content);
        ptr = ptr->next;
    }
    free(temp);
}   */